import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    input: {
        height: 40,
        borderColor: '#E6F0FA',
        borderWidth: 1,
        marginBottom: 10,
        paddingHorizontal: 8,
        width: '100%',
    },
});
